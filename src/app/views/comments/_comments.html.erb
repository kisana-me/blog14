<div class="comments">
  <% if logged_in? %>
    <% comments = post_all_comments(post) %>
  <% else %>
    <% comments = post_comments(post) %>
  <% end %>
  <% comments.each do |comment| %>
    <%= render 'comments/show', comment: comment, top: true %>
  <% end %>
</div>